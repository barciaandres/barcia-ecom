<h1>Nuestros Productos</h1>

<div class="row">
    {{#each products.docs}}
    <div class="col-md-4 mb-4">
        <div class="card">
            <img src="{{this.thumbnail}}" class="card-img-top" alt="{{this.title}}" style="height: 200px; object-fit: cover;">
            <div class="card-body">
                <h5 class="card-title">{{this.title}}</h5>
                <p class="card-text">{{this.description}}</p>
                <p class="card-text"><strong>Categoría:</strong> {{this.category}}</p>
                <p class="card-text"><strong>Precio:</strong> ${{this.price}}</p>
                <p class="card-text"><strong>Stock:</strong> {{this.stock}}</p>
            </div>
        </div>
    </div>
    {{else}}
    <p>No hay productos para mostrar.</p>
    {{/each}}
</div>
